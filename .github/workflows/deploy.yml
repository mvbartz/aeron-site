name: Deploy para Hostinger

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notificar Hostinger
        run: curl -s "https://aeronsolutions.com.br/webhook.php?token=aeron_deploy_2026" || true
